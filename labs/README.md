# Labs

This directory contains small, repeatable exercises that may be discarded and rebuilt. Move stable code that evolves across weeks into `projects/`.

## Rules

- Every lab must state its goal, run instructions, correctness oracle, and acceptance criteria.
- Every performance lab must record shape, dtype, device, warmup, synchronization, and sample count.
- Do not commit model weights or large profiler outputs.

