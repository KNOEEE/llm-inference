[pytest]
markers =
    cpu: runs on a CPU-only environment
    mps: requires PyTorch MPS on Apple Silicon
    mlx: requires Apple Silicon and MLX
    cuda: requires an NVIDIA CUDA environment
    slow: long-running integration or benchmark test

