# Nano-vLLM Environment

Install Nano-vLLM's PyTorch, Triton, FlashAttention, and NCCL dependencies in a dedicated environment. Start with one GPU, TP=1, and eager mode.

Record the fork URL, commit, model path, and launch parameters. Enable CUDA Graphs only after correctness is stable.

