# M4 / MLX Environment

Tiny-LLM uses the PDM and dependency configuration in its upstream repository. This directory records only the machine, macOS version, Xcode/Metal version, Python version, MLX version, mlx-lm version, and Tiny-LLM commit.

Do not mix CUDA dependencies into this environment. Start with Qwen3-0.6B.

