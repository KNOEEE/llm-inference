# Patches

Store a small patch only when a fork and commit cannot express the change. Include the upstream name, commit, and purpose in the filename, and document how to apply and revert it in an adjacent README.

